% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{cpp_sample_simplex_ball}
\alias{cpp_sample_simplex_ball}
\title{Sample every active simplex-sphere component in parallel}
\usage{
cpp_sample_simplex_ball(
  A,
  b,
  V,
  starting_points,
  component_weights,
  center,
  N,
  n_chains,
  check_interval,
  max_iterations,
  walk_length,
  burnin,
  psrf_target,
  ess_target,
  walk,
  regcw_tau,
  max_reflections,
  seed = NULL
)
}
\arguments{
\item{A, b}{Finite simplex half-space representation `A * x <= b`.}

\item{V}{Simplex vertices stored column-wise.}

\item{starting_points}{Matrix with one feasible starting point per column.}

\item{component_weights}{Non-negative component surface-area weights.}

\item{center}{Centre of the unit sphere.}

\item{N}{Exact total number of samples to return. The native wrapper uses
deterministic largest-remainder allocation across components.}

\item{n_chains}{Number of independent diagnostic chains per component.}

\item{check_interval}{Base retained draws per chain between diagnostic
checks. It is increased when necessary to provide each component pool.}

\item{max_iterations}{Maximum retained draws per chain.}

\item{walk_length}{Number of transitions between retained draws.}

\item{burnin}{Number of discarded transitions per chain.}

\item{psrf_target, ess_target}{Convergence thresholds.}

\item{walk}{Either `"GCW"` or `"ReGCW"`.}

\item{regcw_tau}{Reflective-walk trajectory scale.}

\item{max_reflections}{Reflective-walk cap; zero selects the native default.}

\item{seed}{Optional non-negative master RNG seed.}
}
\value{
A list containing the exact combined sample matrix, one-based
  sample-component labels, normalized weights and counts, per-component
  convergence results, aggregate PSRF/ESS, convergence status and warnings.
}
\description{
This is the native multicomponent sampler used by the high-level
`sample_simplex_ball()` pipeline. Each active component receives its own
RNG stream and convergence-controlled set of chains. Components are run
concurrently; PSRF and ESS are still evaluated only among chains targeting
the same connected component.
}
\keyword{internal}
