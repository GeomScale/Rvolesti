% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplex_ball.R
\name{find_components}
\alias{find_components}
\title{Find connected components of a simplex--sphere intersection}
\usage{
find_components(V, x0, radius = 1, tolerance = 1e-10)
}
\arguments{
\item{V}{A numeric matrix containing simplex vertices in columns
(`dimension` by `dimension + 1`). Its transpose is also accepted.}

\item{x0}{The ball centre.}

\item{radius}{A strictly positive ball radius. The sampling API uses the
canonical unit radius.}

\item{tolerance}{A strictly positive numerical tolerance.}
}
\value{
An object of class `simplex_ball_components`, containing
  `components`, `adjacency`, `active`, and `active_vertices` when supplied
  by the native implementation.
}
\description{
Builds the simplex 1-skeleton after removing edges that intersect the ball
and vertices strictly inside it. Returned vertex indices follow R's
one-based convention.
}
\examples{
V <- rbind(
  c(-2, -2,  2,  2),
  c(-0.2, 0.2, -0.2, 0.2),
  c(-0.2, 0.2,  0.2, -0.2)
)
components <- find_components(V, x0 = c(0, 0, 0))
length(components$components)

}
