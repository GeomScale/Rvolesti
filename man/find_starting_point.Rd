% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplex_ball.R
\name{find_starting_point}
\alias{find_starting_point}
\title{Find a feasible starting point on one simplex--sphere component}
\usage{
find_starting_point(
  V,
  component,
  A,
  b,
  x0,
  interior_point = NULL,
  radius = 1,
  tolerance = 1e-10
)
}
\arguments{
\item{V}{A numeric matrix containing simplex vertices in columns. Its
transpose is also accepted.}

\item{component}{One non-empty vector of one-based simplex vertex indices.}

\item{A}{A finite constraint matrix.}

\item{b}{A finite right-hand-side vector defining \eqn{A x \leq b}.}

\item{x0}{The ball centre.}

\item{interior_point}{An optional point strictly inside the simplex--ball
body. If `NULL`, the native implementation computes an approximate
Chebyshev centre.}

\item{radius}{A strictly positive ball radius. The sampling API uses the
canonical unit radius.}

\item{tolerance}{A strictly positive numerical tolerance.}
}
\value{
A numeric vector on the requested spherical component.
}
\description{
Intersects rays from an interior point toward the component vertices with
the sphere and verifies every simplex inequality.
}
\examples{
V <- rbind(
  c(-2, -2,  2,  2),
  c(-0.2, 0.2, -0.2, 0.2),
  c(-0.2, 0.2,  0.2, -0.2)
)
A <- rbind(c(1, 10, 10), c(1, -10, -10),
           c(-1, 10, -10), c(-1, -10, 10))
b <- rep(2, 4)
component_data <- find_components(V, c(0, 0, 0))
start <- find_starting_point(
  V, component_data$components[[1]], A, b, c(0, 0, 0)
)

}
